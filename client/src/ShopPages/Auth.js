import React, {useContext} from 'react';
import AuthForm from "../components/UI/AuthForm/AuthForm";

const Auth = () => {

    return (
        <div>
            <AuthForm/>
        </div>
    );
};

export default Auth;